# 07. UI/UX 指針

- ratatui + crossterm を用いた TUI。上部に情報バー（ファイル名、サイズ、カーソルオフセット、編集モード）、中央にメインビュー、下部にコマンドライン/メッセージ領域。
- Emacs 風のキーバインドをデフォルト（`Ctrl+S` 保存、`Ctrl+Z` Undo、`Ctrl+F/B` 移動）にし、`config.toml` で再定義可能。
- マウス入力はオプション（スクロールのみ）。
